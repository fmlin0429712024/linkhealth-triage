name: Deploy LinkHealth VAS

on:
  push:
    branches: [main]
    paths:
      - 'triage-dsh-plugin/**'
      - 'linkhealth-gui-plugin/**'
      - 'deploy/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build the deploy unit (self-contained profile + plugins)
        run: |
          set -euo pipefail
          SHA="${GITHUB_SHA::7}"
          mkdir -p dist/profile/plugins dist/profile/node_modules
          cp -r deploy/profile-linkhealth/package.json deploy/profile-linkhealth/cordis.patch.yml dist/profile/
          cp -r triage-dsh-plugin dist/profile/plugins/
          cp -r linkhealth-gui-plugin dist/profile/plugins/
          # relative symlink — keeps the release directory self-contained
          ln -s ../plugins/linkhealth-gui-plugin dist/profile/node_modules/linkhealth-gui-plugin
          tar -czf "linkhealth-${SHA}.tar.gz" -C dist profile
          echo "release=linkhealth-${SHA}.tar.gz" >> "$GITHUB_ENV"

      - name: Upload release to the VM
        run: |
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > /tmp/deploy_key && chmod 600 /tmp/deploy_key
          scp -i /tmp/deploy_key -o StrictHostKeyChecking=no \
            "${{ env.release }}" \
            "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/opt/linkhealth/incoming/"

      - name: Deploy (unpack → switch symlink → restart → verify)
        run: |
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > /tmp/deploy_key && chmod 600 /tmp/deploy_key
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no \
            "${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}" \
            "sudo bash /opt/linkhealth/scripts/deploy.sh '${{ env.release }}' '${{ secrets.DEEPSEEK_API_KEY }}'"
